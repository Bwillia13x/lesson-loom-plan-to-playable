/** Per 16_INTERACTION_AND_MOTION_SPEC.md — absolute ms from weave CTA press (steps 0–6). */
export const WEAVE_STEP_DELAYS_MS = [100, 220, 340, 460, 580, 700, 820] as const;

/** Signal cards complete reveal after student-app node (820ms) activates. */
export const WEAVE_SIGNAL_REVEAL_DELAY_S = 0.9;

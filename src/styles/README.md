# Styles partials

Entry point: `src/styles.css` (imports in cascade order).

| File | Contents |
|------|----------|
| `tokens.css` | Design tokens, reset, global reduced-motion |
| `layout.css` | App shell, nav, topbar, weave stepper |
| `primitives.css` | Sections, panels, buttons, badges, progress rail |
| `motion.css` | Weave path, post-weave student highlight |
| `sections.css` | Feature sections (hero, garden, teacher, export, footer) |
